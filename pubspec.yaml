name: expense_tracker
description: "Expense Tracker App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.0
  path: ^1.8.3
  flutter_sms_inbox: ^1.0.3
  permission_handler: ^11.0.1
  intl: ^0.18.1
  cupertino_icons: ^1.0.6
  system_alert_window: ^2.0.11

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
